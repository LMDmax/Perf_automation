{
  "name": "yes",
  "version": "1.0.0",
  "description": "this is for cypress practice",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:report": "npx mochawesome-merge cypress/reports/*.json > cypress/reports/output.json && npx marge cypress/reports/output.json --reportDir cypress/reports --inline",
    "test": "npm run cypress:run && npm run cypress:report"
  },
  "author": "priyanka",
  "license": "ISC",
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.2.5",
    "cypress": "^13.14.2",
    "cypress-downloadfile": "^1.2.4",
    "cypress-file-upload": "^5.0.8",
    "cypress-mailosaur": "^2.17.0",
    "cypress-websocket-server": "^0.1.2",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0",
    "xlsx": "^0.18.5"
  },
  "dependencies": {
    "chai": "^5.1.1",
    "cypress-xpath": "^2.0.1",
    "mocha": "^10.7.3"
  }
}
